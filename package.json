{
  "name": "pdf-processor",
  "version": "1.0.0",
  "description": "PDF processor full-stack application - monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "install:all": "npm install",
    "dev": "concurrently \"npm run dev --workspace=backend\" \"wait-on http-get://localhost:3000/config && npm run dev --workspace=frontend\"",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "build": "npm run build:backend && npm run build:frontend",
    "build:frontend": "npm run build --workspace=frontend",
    "build:backend": "npm run build --workspace=backend",
    "start": "concurrently \"npm run start --workspace=backend\" \"npm run start --workspace=frontend\"",
    "start:prod": "concurrently \"npm run start:prod --workspace=backend\" \"npm run start:prod --workspace=frontend\"",
    "clean": "npm run clean --workspace=backend && npm run clean --workspace=frontend",
    "lint": "npm run lint --workspace=frontend && npm run lint --workspace=backend",
    "test": "npm run test --workspace=frontend && npm run test --workspace=backend"
  },
  "keywords": [
    "pdf",
    "processor",
    "react",
    "node",
    "express",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "concurrently": "^9.2.1",
    "wait-on": "^9.0.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-athena": "^3.997.0",
    "@aws-sdk/client-s3": "^3.997.0",
    "cpx2": "^8.0.0",
    "csv-parse": "^6.1.0"
  }
}
