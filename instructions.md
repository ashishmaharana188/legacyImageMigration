Two major changes and refactoring are to be done. Plan, Analyse, Organise the approach as its a goign to be big change.

Your task is this:

A. Fully restructure the project's folder structure based on the "docs/gemini_api_folder_structure" which is supposed to be followed as the context for a API based fodler strucutre. Carefully analysing current project's flow.
B. The database.ts is a big file that contants multiple code lines in one file for multiple apis, keeping in mind the restructure mnake sure to redesign the file my moving code to appropiate api direcotries.

Common Points to consider while making the changes:

1. The project's strucutre now is very random as per current user. I want to approach this from a fully production grade view.
2. Access "docs/gemini_api_folder_structure" and carefully analyse the structure.
3. Carefully analyse the root folder structure and frontend/backend through "folder_structure/" directory at root.
4. Use these file as a summary context of the current flow and desgin of the project "docs/architecture-design-document".
   Dont take data in this file as the absolute truth and form a confirmation bias, always use the folder_structure to access requried files and read them fresh when in confusion.
5. Use this fie as the best practices ".gemini/GEMINI.md".
