> check the updateTransactionQuery for ts.clientId in where condition, since this applcation is going to be used in client specifc ways. I want a front end option that          │
│   queries all the client codes from select* from fund.client_master and dispalys them in a dropdown with select client code as the title. once user selects the client           │
│   code the full application is set to use this clientCode or its associated id where ever its needed. This initla client code fetch needs to happen from the database by         │
│   connecting to it and running the query. can this be done?  